# Projeto em C

Este projeto utiliza um `Makefile` para compilar o código-fonte localizado em `src/main.c`, gerando o executável na pasta `bin/`.

## Estrutura do Projeto

```text
project/
├── src/
│   └── main.c
├── bin/
├── db/
│   └── users.txt
├── .gitignore
├── Makefile
├── Projecto_PPP_2025_26.pdf
├── readme.md
└── run.sh